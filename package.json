{
    "name": "pi-monorepo",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*",
        "packages/web-ui/example",
        "packages/coding-agent/examples/extensions/with-deps",
        "packages/coding-agent/examples/extensions/custom-provider-anthropic",
        "packages/coding-agent/examples/extensions/custom-provider-gitlab-duo",
        "packages/coding-agent/examples/extensions/custom-provider-qwen-cli"
    ],
    "scripts": {
        "clean": "bun run clean --workspaces",
        "build": "turbo build",
        "dev": "turbo dev",
        "dev:tsc": "turbo dev:tsc",
        "check": "biome check --write --error-on-warnings . && tsgo --noEmit && bun run check:browser-smoke && cd packages/web-ui && bun run check",
        "check:browser-smoke": "bun scripts/check-browser-smoke.mjs",
        "profile:tui": "node scripts/profile-coding-agent-node.mjs --mode tui",
        "profile:rpc": "node scripts/profile-coding-agent-node.mjs --mode rpc",
        "test": "turbo test",
        "version:patch": "bun run version:patch -ws && node scripts/sync-versions.js && rm -rf node_modules packages/*/node_modules bun.lock && bun install",
        "version:minor": "bun run version:minor -ws && node scripts/sync-versions.js && rm -rf node_modules packages/*/node_modules bun.lock && bun install",
        "version:major": "bun run version:major -ws && node scripts/sync-versions.js && rm -rf node_modules packages/*/node_modules bun.lock && bun install",
        "version:set": "bun run version -ws",
        "prepublishOnly": "bun run clean && bun run build && bun run check",
        "publish": "bun run prepublishOnly && bun publish -ws --access public",
        "publish:dry": "bun run prepublishOnly && bun publish -ws --access public --dry-run",
        "release:patch": "node scripts/release.mjs patch",
        "release:minor": "node scripts/release.mjs minor",
        "release:major": "node scripts/release.mjs major",
        "prepare": "husky"
    },
    "devDependencies": {
        "@biomejs/biome": "2.4.9",
        "@types/node": "^25.5.0",
        "@typescript/native-preview": "7.0.0-dev.20260326.1",
        "concurrently": "^9.2.1",
        "esbuild": "^0.27.4",
        "husky": "^9.1.7",
        "oxlint": "^1.57.0",
        "oxlint-tsgolint": "^0.17.4",
        "shx": "^0.4.0",
        "tsx": "^4.21.0",
        "turbo": "^2.8.20",
        "typescript": "^6.0.2"
    },
    "engines": {
        "node": ">=20.0.0"
    },
    "packageManager": "bun@1.3.11",
    "version": "0.0.3",
    "dependencies": {
        "@mariozechner/jiti": "^2.6.5",
        "@adamliang0/pi-coding-agent": "workspace:*",
        "get-east-asian-width": "^1.5.0"
    }
}
